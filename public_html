😢😢 المصدر الرئيسي للرسائل

البوت يستقبل الرسائل من لوحة iVasms فقط (وليس من عدة لوحات). يتم ذلك عبر:

```python
# اللوحة الوحيدة المستخدمة
IVASMS_DASHBOARD = {
    "name": "iVasms",
    "type": "ivasms",
    "login_url": "https://ivas.tempnum.qzz.io/login",
    "sms_api_endpoint": "https://ivas.tempnum.qzz.io/portal/sms/received/getsms",
    "username": "asmeralselwi103@gmail.com",
    "password": "Mohammed Saeed 123",
    # ... 
}
```

آلية استقبال الرسائل خطوة بخطوة

الخطوة 1: تسجيل الدخول إلى اللوحة

```python
def login_to_ivasms():
    # تسجيل الدخول باستخدام البريد وكلمة المرور
    # الحصول على CSRF token وجلسة صالحة
```

الخطوة 2: جلب الرسائل بشكل دوري

```python
def fetch_ivasms_messages():
    # ترسل طلب إلى API اللوحة كل 3 ثوانٍ
    # تجلب الرسائل الجديدة من آخر ساعة فقط
    
    # 1. تجلب ملخص الرسائل
    summary_resp = session.post(sms_api_url, data=payload)
    
    # 2. تستخرج الأرقام التي وصلتها رسائل
    numbers_resp = session.post(numbers_url, data=numbers_payload)
    
    # 3. تجلب نص كل رسالة
    sms_resp = session.post(sms_details_url, data=sms_payload)
```

الخطوة 3: معالجة الرسائل الجديدة

```python
# في الحلقة الرئيسية main_loop():
messages = fetch_ivasms_messages()

for msg in messages:
    msg_id = msg['id']
    
    if msg_id not in sent_messages:  # رسالة جديدة؟
        # استخراج البيانات
        number = clean_number(msg['number'])
        sms_text = msg['text']
        
        # إرسال للمستخدم والجروب
        send_otp_to_user_and_group(date_str, number, sms_text)
        
        sent_messages.add(msg_id)  # حفظ لمنع التكرار
```

🤦‍♀️ معالجة وتحليل الرسالة

عند وصول رسالة جديدة، يتم تحليلها:

```python
def send_otp_to_user_and_group(date_str, number, sms):
    # 1. استخراج الكود (OTP)
    otp_code = extract_otp(sms)
    
    # 2. معرفة الدولة (من الرقم)
    country_name, country_flag, country_code = get_country_info(number)
    
    # 3. معرفة الخدمة (WhatsApp, Facebook, إلخ)
    service = detect_service(sms)
    
    # 4. البحث عن المستخدم صاحب الرقم
    user_id = get_user_by_number(number)
    
    # 5. تسجيل في قاعدة البيانات
    log_otp(number, otp_code, sms, user_id)
```

🫦 إرسال OTP للمستخدم والجروب

```python
# للمستخدم الخاص
bot.send_message(user_id, f"✨ كود التفعيل: {otp_code}")

# للجروب العام
send_to_telegram_group(text, otp_code)
```

😒 منع التكرار

يتم حفظ معرفات الرسائل المرسلة في ملف JSON:

```python
SENT_MESSAGES_FILE = "ivasms_sent_messages.json"
# حفظ آخر 1000 رسالة لمنع الإرسال المكرر
```

--